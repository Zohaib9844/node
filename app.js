const names=require("./name.js");
let namer=(name)=>{
    console.log("The name is : "+name);
    return name;
}
namer(names.dave);
namer("Zahid");
console.log(module);
console.log(names);
namer(names.person);

// OS module

